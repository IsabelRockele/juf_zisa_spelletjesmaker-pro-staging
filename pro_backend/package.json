{
  "name": "zisa-spelletjesmaker-pro-backend",
  "private": true,
  "type": "commonjs",
  "main": "lib/index.js",
  "files": [
    "lib"
  ],
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rimraf lib",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.0.1",
    "pdfkit": "^0.17.2"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "@types/pdfkit": "^0.17.2",
    "rimraf": "^5.0.5",
    "typescript": "^5.7.3"
  }
}
